#!/bin/bash

# Run this in a directory to create an empty vim plugin with filenames that match user input on first run.
# This file will delete itself after running.

# VARIABLES
directories="ftplugin ftdetect indent syntax"
files="ftplugin

for dir in $directories; do
    mkdir dir
done


